Dependencies for Mulholland.
Right now this just includes Caterwaul, the standard extension, and the
Caterwaul Bloom filter library. These are used by and baked into the Mulholland
compiler/interpreter (mh). From then on, mh can clone itself independently of
these dependencies.
